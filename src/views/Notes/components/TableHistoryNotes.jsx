import React from "react";
import { connect } from "react-redux";

const TableHistoryNotes = () => {
  return (
    <div>
      <h2>Table History Notes</h2>
    </div>
  );
};

const mapStateToProps = (state) => ({});

export default connect(mapStateToProps)(TableHistoryNotes);
